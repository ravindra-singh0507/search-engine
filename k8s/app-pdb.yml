apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: search-engine
  namespace: search-engine
  labels:
    app: search-engine
    component: api
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: search-engine
      component: api
